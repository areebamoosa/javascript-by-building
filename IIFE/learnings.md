# 📚 Learnings from IIFE Mini-Project

- **IIFE** stands for **Immediately Invoked Function Expression**.

- Syntax: `(function() { /* code */ })();` → function runs **right away**.
- Wrapping the function in `()` makes it a **function expression** instead of a declaration.
- Adding `()` at the end **invokes the function immediately**.

- **Example learned:** Showed a greeting alert as soon as the page loads using an IIFE.
