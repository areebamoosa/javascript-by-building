# 📚 Learnings from Mini Shopping Cart App

- Practiced **DOM selection** using `querySelectorAll`, `getElementById`, and `closest()`.

- Learned to **extract and clean price text** using `.replace()` and `parseInt()`.

- Created **product objects** with unique IDs using `Date.now()` + `Math.random()`.

- Stored cart items in an **array** and displayed them dynamically with `innerHTML`.

- Calculated the **total price** by looping through the cart array.

- Added a **remove button** for each cart item and updated the UI after removal.

- Understood how to **re-render the DOM** after changes in the data.
