# 📚 Learnings from Navigator Object Mini-Project

- **Browser Info**:

  - `navigator.userAgent` → shows browser name, version, and platform info.

- **Online/Offline Status**:

  - `navigator.onLine` → tells if the user is connected to the internet.

  - Displays “Online” if true, “Offline” if false.

- **Language Info**:

  - `navigator.language` → shows the user’s preferred language (like `en-US`).

- **Concepts Practiced**:

  - Accessing browser/device info using the `navigator` object.

  - Displaying live device info on the webpage.
  - Understanding how built-in browser APIs provide environmental data.

## Notes

### Navigator object

navigator is a built-in JavaScript object that holds info about the browser and the device you're using.

It lets you see details like the browser name/version, language, and internet connection status.
