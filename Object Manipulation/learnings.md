# 📚 Learnings from Object Manipulation Mini-Project

- **Creating an Object**:

  - Stored user/student data as key-value pairs in a single object.

- **Accessing Object Properties**:

  - Used `for...in` loop to iterate over the object keys.
  - Accessed values using `object[key]` notation.

- **Displaying Data on Page**:

  - Dynamically created `<p>` elements for each key-value pair.
  - Appended them to a container in the HTML when the button was clicked.

- **Concepts Practiced**:
  - Understanding objects in JavaScript.
  - Object property iteration with `for...in`.
  - DOM manipulation using `document.createElement` and `appendChild`.
