# 📚 Learnings from Location Object Mini-Project

- **Current Page Info**:

  - `location.href` → full URL of the current page.
  - `location.hostname` → domain name (like `example.com`).
  - `location.protocol` → `http:` or `https:`.

- **Page Navigation**:

  - `location.reload()` → reloads the current page.
  - `location.assign("url")` → redirects browser to another page.

- **Concept Practiced**:

  - Updating DOM elements to display live page info.
