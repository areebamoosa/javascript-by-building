# 📚 Learnings from Toggle Themes Project

- Practiced **DOM selection** using `getElementById`.

- Learned to **toggle classes dynamically** using `classList.toggle()`.

- Used `classList.contains()` to **check the current state** (light or dark).

- Updated button text dynamically with `.textContent` based on the theme.

- Understood **real-time style changes** by adding/removing CSS classes.
