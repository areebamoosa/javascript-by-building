# Learnings from To-Do App 📝

- Practiced **DOM selection** using `getElementById` and `querySelector`.

- Learned to **create elements dynamically** using `document.createElement`.

- Used `.appendChild()` to add new list items into the DOM.

- Cleared input field after adding a task by  `.value = ""`.

- Used `.innerHTML = ""` to remove all tasks at once.

- Understood the **connection between HTML elements and JS events** using `addEventListener`.
