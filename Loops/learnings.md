# 📚 Learnings from Loops Project

- Practiced different **loop types**: `for`, `while`, `do-while`, `for-of`, and `for-in`.

- Understood how **`for` and `while` loops** use index-based iteration over arrays.

- Learned that **`for-of`** directly accesses array values without needing an index.

- Used **`for-in`** to loop through object keys and access corresponding values.

- Strengthened understanding of how **loops interact with arrays and objects** in the DOM.

- Used `document.createElement` and `.appendChild()` to add list items dynamically.
